package org.jcryptool.visual.rsa_elgamal.textbook.generic;

public interface IGenericProcess<T extends IState> extends IProcess<T> {

	// inherits the default validation method of IProcess, which does never fail
	
}
